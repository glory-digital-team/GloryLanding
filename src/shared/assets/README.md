# shared/assets

Статичные ресурсы, переиспользуемые в коде (иконки-компоненты, SVG, изображения),
которые импортируются из TS/TSX.

> Файлы, доступные по прямому URL (favicon, og-image, robots.txt), кладите в `/public`.
